#!/usr/bin/python3

def number_keys(a_dictionary):
    # Initialize a counter to store the number of keys
    count = 0

    # Iterate through the keys in the dictionary and increment the count for each key
    for key in a_dictionary:
        count += 1

    return count
